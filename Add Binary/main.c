#include <stdio.h>
#include <string.h>

char* addBinary(char* a, char* b) {
    
    char *  max = strlen(a)>strlen(b)?a:b;
    for(int i = 0 ; i < strlen(max) ; i ++){

    }

}
int main() {
    printf("Hello, World!\n");
    return 0;
}